defmodule Helpdesk.Support.Changes.CreateInitialMessage do
  use Ash.Resource.Change

  @impl true
  def change(changeset, _opts, _context) do
    # Implementation for creating initial message
    changeset
  end
end
